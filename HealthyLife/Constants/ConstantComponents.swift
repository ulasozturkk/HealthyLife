

import Foundation
import UIKit

enum ConstantComponents {
  static let backButton = customButton(backGroundColor: .white)
  static let iconImageView = customImageView(image: UIImage(named: "appIcon500.png")!)
}
